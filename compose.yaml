services:
  goddns:
    build:
      context: .
    image: goddns:local
    container_name: goddns
    restart: unless-stopped
    read_only: true
    network_mode: host
    volumes:
      - ./config.yaml:/app/config.yaml:ro
    command:
      - -config
      - /app/config.yaml
